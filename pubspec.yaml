name: pos
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  syncfusion_flutter_charts: ^26.2.5+1
  table_calendar: ^3.1.2
  cupertino_icons: ^1.0.2
  logger: ^2.0.2+1
  dio: ^5.4.1
  custom_pin_screen: ^1.0.1
  iconsax: ^0.0.8
  cached_network_image: ^3.2.0
  equatable: ^2.0.5
  connectivity_plus: ^6.0.3
  flutter_secure_storage: ^9.0.0
  get_storage: ^2.1.1
  flutter_keyboard_visibility: ^6.0.0
  shimmer: ^3.0.0
  flutter_neumorphic: ^3.2.0
  fluentui_system_icons: ^1.1.231
  flutter_screenutil: ^5.9.0
  font_awesome_flutter: ^10.7.0
  ionicons: ^0.2.2
  google_fonts: ^6.2.0
  flutter_dropdown: ^1.0.1+hotfix.1
  custom_calender_picker: ^0.1.1
  full_calender: ^1.2.12
  flutter_datetime_picker: ^1.5.1
  flutter_picker: ^2.1.0
  pie_chart: ^5.0.0
  flutter_svg: ^2.0.10+1
  flutter_data_table: ^1.1.2
  flutter_easyloading: ^3.0.5
  flutter_staggered_animations: ^1.1.1
  hovering: ^1.0.4
  animated_card: ^2.0.0
  network_to_file_image: ^6.0.1
  flutter_hooks: ^0.20.5
  get: ^4.6.6
  fl_chart: ^0.68.0
  flutter_math_fork: ^0.7.2
  flutter_simple_calculator: ^2.3.0
  flutter_awesome_calculator: ^1.0.0+1
  flutter_xlider: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/table6.svg
    - assets/images/table2.svg
    - assets/images/table4.svg
    - assets/images/table5.svg
    - assets/images/table3.svg
    - assets/images/user.png
    - assets/images/logo.png
    - assets/images/image_panier.png
    - assets/images/delivery.png
